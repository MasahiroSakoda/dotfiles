---
name: lint

on:
  push:
    branches:
      - main

jobs:
  shellcheck:
    name: 'ShellCheck'
    runs-on: ubuntu-latest
    steps:
    - uses: 'actions/checkout@v3'
    - name: 'Run shellcheck'
      uses: 'azohra/shell-linter@master'
      env:
        SHELLCHECK_OPTS: -P **/*.sh
