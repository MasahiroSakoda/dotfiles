#!/usr/bin/env bash
# -*-mode:sh-*- vim:ft=sh

set -eo pipefail

# Configure global version
rtx use -g ruby@{{ .rtx.versions.python }}

# Upgrade pip
python3 -m pip install --upgrade pip
python3 -m pip install pynvim
