-- -*-mode:lua-*- vim:ft=lua
local schemes = {}
---@diagnostic disable: exp-in-action, undefined-global, unknown-symbol, miss-exp
{{- range .terminal.wezterm.themes | uniq }}
table.insert(schemes, {{ . | quote }})
{{- end }}

return {
  color_schemes = schemes
}
